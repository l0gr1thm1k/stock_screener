{
    DEFAULT {
        logging.level = "debug"
        logging.log_requests = true
        logging.log_responses = true
    }

    LOCAL {
        logging.level = "debug"
        logging.log_requests = true
        logging.log_responses = true
    }

    DEVELOPMENT {
        logging.level = "debug"
        logging.log_requests = false
        logging.log_responses = false
    }

    PRODUCTION {
        logging.level = "info"
        logging.log_requests = false
        logging.log_responses = false
    }
}
